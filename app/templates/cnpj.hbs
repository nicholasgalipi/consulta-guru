{{page-title "Consulte dados de um CNPJ onlione grátis em poucos segundos!"}}
<Header />

<Search @cnpj={{this.cnpj}}
        @handleCnpj={{this.handleCnpj}}
        @handleQuerry={{this.handleQuerry}}
        @cnpjIsValid={{this.cnpjIsValid}}  
        />

<Response   @legalEntity={{@model.legalEntity}} 
            @mainActivity={{@model.mainActivity}} 
            @secondaryActivity={{@model.secondaryActivity}} 
            @shareCapital={{@model.shareCapital}}
            />

<Footer />
